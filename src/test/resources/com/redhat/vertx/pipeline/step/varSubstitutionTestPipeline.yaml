- name: tokenize
  copy:
    register: words
    vars:
      from: '{{ doc.intro|split|tojson }}'
- name: first word
  copy:
    register: first_word
    vars:
      from: '{{ doc.words[0] }}'
- name: fourth word
  copy:
    register: fourth_word
    vars:
      from: '{{ doc.words[3] }}'
